
#include <components/GeometryEvaluator.h>

void GeometryEvaluator::updateGeometries(const GeometryPool& geometryPool)
{
    // evaluate new geometries in geometry pool
    // update dirty geometries in geometry pool
    // delete deleted geometries in geometry pool
}
